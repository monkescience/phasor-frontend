apiVersion: v1
kind: Service
metadata:
  name: {{ include "phasor-frontend.fullname" . }}
  labels:
    {{- include "phasor-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "phasor-frontend.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "phasor-frontend.fullname" . }}-canary
  labels:
    {{- include "phasor-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "phasor-frontend.selectorLabels" . | nindent 4 }}
