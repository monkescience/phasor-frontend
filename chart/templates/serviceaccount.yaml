{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "phasor-frontend.serviceAccountName" . }}
  labels:
    {{- include "phasor-frontend.labels" . | nindent 4 }}
{{- end }}
